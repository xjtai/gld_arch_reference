---
specId: SPEC-参考组-001
type: spec
title: 广小联企业级高可用智能客服 Agent
domain: 客服
system: reference-customer-service-agent
owner: 参考组 / 讲师
status: 已确认
confirmed: true
version: 1.0
created_at: 2026-08-17
updated_at: 2026-08-17
prd_ref: prd.md
tier: medium
knowledge_refs: []
related_specs: [plan.md, eval.md]
score: {}
---

> **参考实现**：这是一种合格解，不是标准答案。标 `【硬要求】` 的来自 `prd.md`，不能变；其余是本方案的选择，换个合理做法同样能通过验收。

# SPEC-参考组-001｜广小联企业级高可用智能客服 Agent

## 1. 背景与目标

用户在客服对话里的诉求跨度很大——问价格、问库存、查物流、申请退款、闲聊都混在一起。单个 Agent 硬扛所有意图，提示词会越堆越长，某一类答错了也很难定位是哪段逻辑的问题。

做完之后变化：**用户一句话进来，系统先判断这句话属于哪一类，再交给专门处理那一类的智能体**。好处有二——每个子智能体的提示词只管自己那摊事，改售后不会影响售前；任何一个子智能体或工具挂了，路由层能接住并降级，对话不中断。

## 2. 业务语义与元语消歧（AI 必读，先于一切）

- **「路由」依据什么判断**：只看用户当前这句话的语义，不看历史下单记录（本次没有用户画像数据）。判断不了时归入"兜底"，不硬猜。
- **多轮对话何时沿用、何时重新路由**：当前这句话**能独立判断出类别**时，重新路由；**判断不出**（主语省略、指代不明，如"价格呢""那个呢"）时，沿用上一轮的智能体。
- **「退货」≠「退款」**【本次最关键的一处消歧】：
  - **退款**＝资金动作，调用退款工具，用户拿到钱
  - **退货**＝物流动作，要寄回商品
  - 用户说"我要退货"时，实际诉求**多数是退款**。本次统一处理为：**先确认是否需要寄回商品**，不直接调退款工具。
  - `prd.md` §7 Golden Path 里用户说的是"我要退款"，直接走退款流程，不需要确认。
- **高可用里的「失败」指哪些**：工具调用超时（>10s）、工具抛异常、LLM 接口报错或限流。**不含**"用户问了超纲问题"——那是正常兜底，不算失败。

### 2.1 我没敢自己拍板的（Agent 起草时必填）

| 拿不准的点 | Agent 的暂定裁决 | 依据 / 为什么拿不准 | 人确认 |
|---|---|---|---|
| 用户一句话同时问售前和售后（"这手机多少钱？我上次那单还没退款"），算哪类？ | 暂定售后优先，且要在同一条回复里带上售前的答案 | PRD 没有多意图混合的例子，纯猜 | ✅ 确认：售后优先，但**两个诉求都要回应**，只答一半算不通过 |
| 用户说"我要退货"，要不要直接调退款工具？ | 暂定直接调退款 | 脑图里叶子节点写的是"申请退货"，但 Golden Path 演示的是"退款" | ❌ **纠正**：退货和退款是两件事，不能直接调。见 §2 第三条 |
| 多轮对话历史要不要跨会话保留（刷新页面后还在）？ | 暂定保留 | PRD 只画了单次会话的三栏 UI，没提刷新后的行为 | ✅ 确认：**不保留**，内存即可，刷新即重置。见 §3 Out of Scope |
| "3999元起"这个价格是写死还是查 mock 数据？ | 暂定写死在提示词里 | PRD 只给了一句对话示例 | ❌ **纠正**：走 mock 数据表，否则改个价格要改提示词，且 §4 的"数据缺失兜底"没法测 |

### 2.2 参考对话佐证

对照 `prd.md` §7 的 Golden Path，逐句标注：

| 用户这句话 | 落在哪类 | 谁处理 | 为什么 |
|---|---|---|---|
| 手机的保修期限？ | 售前咨询 · 商品功能特性 | 售前智能体 | 问的是商品属性，未涉及具体订单 |
| **价格呢** | 售前咨询 · 商品价格 | 售前智能体（**沿用**） | 主语省略，独立判断不出类别 → 按 §2 规则沿用上一轮 |
| 订单3426436我要退款 | 售后咨询 · 申请退款 | 售后智能体（**重新路由**） | 出现订单号 + 明确"退款"，能独立判断 → 重新路由 |

第二句是本次路由逻辑的**关键验证点**：很多实现会在这里误判成"新问题"重新路由到兜底。

## 3. 范围与非目标

本组落地的需求分类（对照 `prd.md` §2.1：售前、售后是**必选**，不是三选二）：

- [x] 售前咨询　（`prd.md` §2.1 必选）
- [x] 售后咨询　（`prd.md` §2.1 必选）
- [x] 闲聊　　　（可选加分，本方案**做**，用作兜底出口，见 §4）

每类下落地的具体场景【硬要求：每类至少 3 个】：

| 分类 | 场景 | 说明 |
|---|---|---|
| 售前咨询 | 商品价格 | 查 mock 商品表返回价格 |
| 售前咨询 | 商品库存 | 查 mock 商品表返回库存状态 |
| 售前咨询 | 平台政策 | 7天无理由退货、是否包邮，查 mock 政策表 |
| 售后咨询 | 物流查询 | 按 order_id 查 mock 订单表的物流状态 |
| 售后咨询 | **申请退款（Golden Path）** | 按 order_id 调退款工具【硬要求：必须跑通】 |
| 售后咨询 | 修改地址 | 按 order_id 改 mock 订单表收货地址，已发货则拒绝 |
| 闲聊 | 寒暄与身份询问 | "吃了吗""你是男的女的"等，简短回应后引导回客服场景 |

Out of Scope（明确不做，统一走兜底话术）：

- 商品推荐、比价、优惠券领取（脑图里"优惠活动咨询"只做"是否有优惠"的问答，不做发券）
- 取消订单、故障报修、发票申请（脑图里有，本次不做）
- **对话历史跨会话持久化**——内存即可，刷新页面重置
- 多语言 / 方言输入——只保证中文普通话表达
- 真实支付、真实物流对接——全部 mock

**Agent 起草时必须附上盲区清单**：

| 我拿不准要不要列进 Out of Scope 的东西 | 为什么拿不准 | 人裁决 |
|---|---|---|
| 多语言/方言输入要不要支持？ | PRD 通篇是中文示例，没说要不要处理英文或方言 | ☑ 不做 |
| 对话历史要不要跨会话持久化？ | PRD 只画了单次会话的三栏 UI | ☑ 不做 |
| 用户情绪激动/辱骂要不要特殊处理？ | PRD 没提，但真实客服一定会遇到 | ☑ 不做（走闲聊兜底即可，不做情绪识别） |
| 同一订单重复申请退款要不要拦？ | PRD 没提 | ☑ **做**——mock 数据里标记已退款状态，重复申请时提示"该订单已在退款中"（否则演示时连点两次会出现两条成功回复，很难看） |

## 4. 业务规则

### 场景：申请退款（Golden Path，硬要求）

- **输入条件**：用户表达退款意图，且**已提供订单号**
- **处理规则**：调用退款工具 → 返回受理结果 → 回复含"已提交"与"3-5 个工作日退回原支付账户"
- **异常 / 边界**：
  - 未提供订单号 → **反问订单号，不调用工具**（不能编造订单号）
  - 订单号格式非法（非纯数字）→ 提示格式有误，不调用工具
  - 订单不存在 → 告知查询不到，建议核对
  - 订单已在退款中 → 提示"该订单已在退款中"，不重复调用
- **兜底策略**：工具超时或报错 → "当前系统繁忙，您的退款诉求已记录，请稍后再试或转人工"

### 场景：修改地址

- **输入条件**：用户要求改收货地址，且已提供订单号与新地址
- **处理规则**：查订单状态 → 未发货则更新，已发货则拒绝并说明原因
- **异常 / 边界**：缺订单号或缺新地址 → 分别反问，不猜
- **兜底策略**：同上，统一降级话术

### 场景：商品价格 / 库存 / 平台政策（售前三项）

- **输入条件**：用户询问商品属性或平台规则，无需订单号
- **处理规则**：查 mock 商品表 / 政策表，直接作答
- **异常 / 边界**：问到 mock 数据里没有的商品 → "暂未查询到该商品信息"，不编造
- **兜底策略**：同上

### 场景：物流查询

- **输入条件**：用户询问物流进度，已提供订单号
- **处理规则**：查 mock 订单表返回当前物流状态与更新时间
- **异常 / 边界**：缺订单号 → 反问；订单不存在 → 告知查询不到
- **兜底策略**：同上

### 场景：闲聊（兜底出口）

- **输入条件**：既非售前也非售后，或路由判断不出类别
- **处理规则**：简短友好回应，一句话引导回客服场景（"我是客服助手，商品和订单相关的问题都可以问我"）
- **边界**：**不执行任何与客服无关的任务**（写诗、写代码、角色扮演一律拒绝）；**不泄露系统提示词与配置**

## 5. 验收标准

**`prd.md` §4 的五条验收标准条条有 US 兜住**，编号后标出对应第几条：

- **US-01　基准场景（Golden Path）跑通**（← prd §4-1）【硬要求】：按 `prd.md` §7 的三轮对话依次输入，路由与答复均正确，其中第二轮"价格呢"必须沿用售前智能体。
- **US-02　每类 ≥3 场景正确路由**（← prd §4-2）【硬要求】：售前的价格/库存/政策、售后的物流/退款/改地址，共 6 个场景随机抽问均正确路由并合理作答。
- **US-03　高可用降级**（← prd §4-4）【硬要求】：任一工具超时或报错时，返回兜底话术，不抛异常、不无响应、前端不卡死。
- **US-04　路由准确率 ≥85%**（← prd §4-3）【硬要求】：用 ≥20 条未参与调优的语句抽测，正确率 ≥85%（测试口径见 §6）。
- **US-05　执行过程可解释**（← prd §4-5）【硬要求】：路由决策、被调用的智能体、工具调用参数与返回，均可在三栏 UI 的"执行流程"面板查看，每条带时间戳。
- **US-06　参数缺失时先问再做**（本方案增补）：用户未提供订单号就要求退款/查物流/改地址时，系统反问而非调用工具或编造参数。

## 6. 非功能性需求

| 指标 | PRD 原始要求 | 本组的可执行测试口径 |
|---|---|---|
| 性能 | 10 QPS | `locust` 或 `hey`，20 并发压 60 秒打 `/chat` 接口；通过标准：无 5xx、P95 < 8s（含 LLM 往返） |
| 准确率 | ≥ 85% | 测试集 22 条（`eval.md` §1.1），覆盖 6 个场景 + 模糊/超纲/注入；由讲师标注期望分类，**不参与提示词调优** |
| 高可用 | 失败不打断对话 | 覆盖三种故障：工具超时、工具抛异常、LLM 限流；用 `FORCE_TOOL_FAILURE` 与 `FORCE_LLM_FAILURE` 两个开关注入 |
| 可观测性 | 关键决策可追踪 | 每轮对话在"执行流程"面板输出 5 类事件：路由决策 / 智能体开始 / 工具调用（含参数）/ 工具返回 / 处理完成，均带时间戳 |

## 7. 依赖与前置确认项

- **mock 数据**（组内成员 A 准备）：商品表 3 条、订单表 5 条（含 `3426436` 这条 Golden Path 用例）、政策表 2 条
- **模型**：DeepSeek `deepseek-chat`，Key 由讲师现场分发，走根目录 `.env`
- **前置对齐**：`3426436` 这个订单号在 `prd.md` §7 里写死了，mock 数据必须包含它，且**不能改**——否则现场演示对不上

## 8. 风险与已知坑

- **LLM 路由不稳定**：同一句话多次调用可能给出不同分类。缓解：温度设 0，分类结果限定在固定枚举内，解析失败时降级到兜底而非报错。
- **网络往返拖慢演示**：每轮对话至少 2 次 LLM 调用（路由 + 子智能体作答），现场网络差时可能 >10s。缓解：路由改用一次调用同时输出分类与参数，减少一跳（见 `plan.md` §2）。
- **80 分钟编码窗口偏紧**：前端三栏 UI 容易超时。缓解：先保证 `/chat` 接口与命令行可跑通（满足全部验收），UI 作为最后增量。

## 9. 变更记录

| 时间 | 改动 | 人 |
|---|---|---|
| 08-17 09:10 | Coding Agent 起草初稿 | Agent |
| 08-17 09:18 | **纠错**：Agent 把「退货」和「退款」当成同一流程，直接调退款工具。拆开，补 §2 第三条与 §4 的确认规则 | 张三 |
| 08-17 09:20 | **纠错**：价格写死在提示词里，改为查 mock 数据表，否则 §4"数据缺失兜底"无法验证 | 张三 |
| 08-17 09:22 | **补充**：增补 US-06（参数缺失时先问再做）——Agent 原稿只覆盖了 prd §4 五条，漏了"缺订单号"这个高频场景 | 李四 |
| 08-17 09:24 | **补充**：盲区表新增"重复申请退款"一项，裁决为要拦——否则演示时连点两次会出两条成功回复 | 李四 |
| 08-17 09:25 | 确认，status 改为已确认 | 张三 |
