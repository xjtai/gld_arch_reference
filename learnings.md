---
specId: LEARNINGS-参考组-001
type: learnings
parent_spec: spec.md
plan_ref: plan.md
tasks_ref: tasks.md
eval_ref: eval.md
domain: 客服
system: reference-customer-service-agent
owner: 参考组 / 讲师
status: 已确认
confirmed: true
version: 1.0
updated_at: 2026-08-17
writeback_status: N/A
writeback_pr: N/A
related_specs: []
score: {}
---

> **参考实现**：这五条**全部可以倒查回前四份文档的变更记录**——每条的"问题"都能在某份文档的某一行找到出处。这才是这份文件该有的样子；如果你们组的 learnings 写出来跟变更记录对不上，说明是编的。

# LEARNINGS-参考组-001｜经验沉淀

## 这几条是怎么被找出来的

Coding Agent 起草时填写：从下面各处挑，标准是**影响范围大**或**最容易在下次项目里重犯**。

| 来源 | 挑出几条 | 说明 |
|---|---|---|
| `spec.md` §9 变更记录 | 1 条（L-01） | 4 条记录里，"退货≠退款"影响最大——它改变了业务规则本身 |
| `plan.md` §8 变更记录 | 2 条（L-02、L-03） | 选型否决、不改清单漏项，都是下次必然再遇到的 |
| `tasks.md` 变更记录（含编码期假设） | 1 条（L-05） | "换机验证"那条价值最高，其余是同类问题 |
| `eval.md` §8 变更记录（两轮评审） | 1 条（并入 L-04） | 补对抗用例那条，与准确率问题合并叙述 |
| `eval.md` §3/§4 未通过门禁、§6 遗留、§7 判定理由 | 1 条（L-04） | 首轮准确率 81.8% 未达标，是唯一一次真正的"没做到" |

## 回流入口对照

| 经验编号 | 回流去处 |
|---|---|
| L-01 | ➜ 团队 AI Coding checklist 新增一条"业务动词必须逐个消歧" |
| L-02 | ➜ 团队选型规范："工期 < 1 天的项目不引入组内无人用过的框架" |
| L-03 | ➜ `plan.md` 模板 §6 增加提示："演示脚本里写死的常量必须锁" |
| L-04 | ➜ `eval.md` 模板 §1.1 增加要求："正例不得超过 70%" |
| L-05 | ➜ `tasks.md` 模板 T-07 的 verify 固化为"换机 clone 验证" |

## L-01　Agent 把「退货」和「退款」当成了同一件事

- **问题**：起草 `spec.md` 时，Agent 看到需求脑图里的叶子节点叫"申请退货"，而 Golden Path 演示的是"退款"，直接把两者合并，写成"用户说退货即调用退款工具"。
- **根因**：PRD 里这两个词同时出现但从未定义关系，Agent 按最省事的方式合并了。**它不是不知道两者有别，是没意识到这里需要问。**
- **修复**：评审时拆开——退款是资金动作、退货是物流动作，用户说"退货"时先确认是否需要寄回商品（`spec.md` §2、§4）。
- **回流**：➜ 团队 AI Coding checklist 新增「**业务动词逐个消歧**」：PRD 里每个动词（删除/取消/关闭/退货/退款）都要单独问一句"它和相邻那个词是不是一回事"。
- **可复用性**：**极高**。这类"近义业务动词被合并"的坑，在订单、账户、审批类需求里几乎必然出现，且**错了不会报错**，要等用户投诉才发现。

## L-02　Agent 选了组里没人会的框架

- **问题**：起草 `plan.md` 时 Agent 选了 LangGraph，理由写得很漂亮（状态管理、可视化）。但组内无人用过。
- **根因**：Agent 按"技术上更合适"选型，**它不知道团队会什么、也不知道工期只有 80 分钟编码时间**——这类约束从来不在文档里。
- **修复**：改为自研 Router + Function Calling，并把 LangGraph 作为"被否决方案"连同理由留在 `plan.md` §2.1。
- **回流**：➜ 团队选型规范新增「**工期 < 1 天的项目，不引入组内无人用过的框架**」；同时在给 Agent 的选型 prompt 里补上"我们组熟悉 X、工期 Y"这类约束。
- **可复用性**：**高**。凡是让 Agent 做技术选型，都要先喂团队能力与工期，否则它会持续推荐"最佳实践"而非"最合适"。

## L-03　不改清单漏了演示脚本里写死的订单号

- **问题**：Agent 起草的 `plan.md` §6 不改清单列了验收标准、Golden Path 效果、分类范围，唯独漏了 mock 订单号 `3426436`。
- **根因**：`3426436` 在 PRD 里是以"示例数据"的面目出现的，看起来可替换。**但它同时被写死在演示脚本里**——这层关系没有任何文档记载。
- **修复**：补进不改清单，并连带补了"Golden Path 三句回复的关键信息点"。评审时人也差点漏掉，是对着 `prd.md` §7 逐句念才发现的。
- **回流**：➜ `plan.md` 模板 §6 增加提示语：「**演示脚本/验收用例里写死的常量，必须列进不改清单**」。
- **可复用性**：**高**。这是"盲区表"机制真正发挥作用的一次——如果不要求 Agent 列出"我不确定该不该锁的东西"，人对着一张只有 3 条的清单，是看不出少了第 4 条的。

## L-04　测试用例全是正例，掩盖了真实准确率

- **问题**：Agent 起草的 `eval.md` §1.1 有 20 条用例，**全部是标准问法的正例**。人评审时补了模糊、超纲、提示注入共 5 类。补完后首轮实测 **18/22 = 81.8%，未达 85% 门槛**——失败的 4 条里有 3 条正是新补的口语化/隐含意图类。
- **根因**：Agent 造用例时会不自觉地"照着自己的实现思路造"，产出的是**它必然能通过的题**。而真实用户不按标准问法说话。
- **修复**：扩充路由提示词里的场景描述（补商品属性词、口语表达示例），复测 **21/22 = 95.5%**。
- **回流**：➜ `eval.md` 模板 §1.1 增加硬性要求：「**正例不得超过 70%，必须含模糊表达、超纲请求、对抗输入三类**」。
- **可复用性**：**极高**，而且**这条如果不做，最容易在现场翻车**——自测 100% 通过，评委随口一问就掉到 85% 以下。补的 C-04/C-05 两条（超纲、提示注入）后来还成了演示加分点。

## L-05　"我这儿能跑"不算跑通

- **问题**：T-07 按新要求换一台机器重新 clone 验证，立刻发现 `src/README.md` 漏写了 `PORT` 环境变量——组内那台机器的 shell 里早就 export 过，一直没暴露。
- **根因**：开发机的环境是**长期积累出来的**，`.env`、shell 变量、已装依赖都可能在文档之外。在同一台机器上验证，等于用带答案的卷子考试。
- **修复**：补全 `src/README.md`；并把 T-07 的 verify 固化为"换一台机器（或干净目录）重新 clone，只照 README 走一遍"。
- **回流**：➜ `tasks.md` 模板 T-07 的 verify 固化为换机 clone 验证。
- **可复用性**：**高**。交付物只要涉及"别人要能跑起来"，这条都成立——评委、新同事、CI 面对的都是干净环境。

## 度量回填

| 阶段 | 计划耗时 | 实际耗时 |
|---|---|---|
| spec.md 起草 | 10min | 8min |
| spec.md 评审（触发 plan.md / eval.md 起草） | 10min | **15min**（消歧那处讨论久了） |
| eval.md 评审 | 10min | 8min |
| plan.md 评审（触发 tasks.md 起草） | 15min | **18min**（框架选型分歧） |
| tasks.md 评审 | 10min | 6min |
| AI Coding | 80min | 80min |
| eval.md 实测 | 20min | **25min**（首轮未达标，多跑一轮） |
| 部署发布 | 15min | 12min |
| learnings 生成确认 + 演示 | 10min | 8min |

**超时的三处都在"人评审"和"实测"环节，编码反而没超**——说明 80 分钟编码窗口是够的，紧的是想清楚和验收。下次可以把评审时间各加 5 分钟，从编码里匀。

| 指标 | 目标 | 实测 |
|---|---|---|
| 路由准确率 | ≥85% | 95.5%（首轮 81.8%） |
| 性能 | 10 QPS | 13.4 QPS |
